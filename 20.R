# Draw an empty plot
plot(1, type = "n", xlab = "", ylab = "", main = "Empty Plot")

# Draw an empty plot with specified axis limits
plot(2, type = "n", xlab = "X-Axis", ylab = "Y-Axis", 
     xlim = c(0, 10), ylim = c(20,100), main = "Empty Plot with Specified Axis Limits")
